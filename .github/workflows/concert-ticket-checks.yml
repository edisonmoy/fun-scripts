name: Concert ticket checks

on:
  push:
    paths:
      - "concert-ticket-alerts/**"
  pull_request:
    paths:
      - "concert-ticket-alerts/**"

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install ruff
        run: pip install ruff

      - name: Run ruff
        working-directory: concert-ticket-alerts
        run: ruff check .

  pytest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        working-directory: concert-ticket-alerts
        run: pip install -r requirements-dev.txt

      - name: Run tests
        working-directory: concert-ticket-alerts
        run: pytest -q
