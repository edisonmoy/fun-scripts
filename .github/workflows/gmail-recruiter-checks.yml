name: Gmail recruiter triage checks

on:
  push:
    paths:
      - "gmail-recruiter-triage/**"
  pull_request:
    paths:
      - "gmail-recruiter-triage/**"

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install ruff
        run: pip install ruff

      - name: Run ruff
        working-directory: gmail-recruiter-triage
        run: ruff check .

  pytest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        working-directory: gmail-recruiter-triage
        run: pip install -r requirements-dev.txt

      - name: Run tests
        working-directory: gmail-recruiter-triage
        run: pytest -q
