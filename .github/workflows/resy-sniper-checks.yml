name: Resy sniper checks

on:
  push:
    paths:
      - "resy-sniper/**"
  pull_request:
    paths:
      - "resy-sniper/**"

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install ruff
        run: pip install ruff

      - name: Run ruff
        working-directory: resy-sniper
        run: ruff check .

  pytest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        working-directory: resy-sniper
        run: pip install -r requirements-dev.txt

      - name: Run tests
        working-directory: resy-sniper
        run: pytest -q
